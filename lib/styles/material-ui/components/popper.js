import { withDisablePortal } from '_utils/material-ui.util'
import '_utils/type-defs.util'

/** @type {MuiComponents['MuiPopper']} */
const overwrite = {}

export const MuiPopper = withDisablePortal(overwrite)
