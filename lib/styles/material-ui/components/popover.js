import { withDisablePortal } from '_utils/material-ui.util'
import '_utils/type-defs.util'

/** @type {MuiComponents['MuiPopover']} */
const overwrite = {}

export const MuiPopover = withDisablePortal(overwrite)
