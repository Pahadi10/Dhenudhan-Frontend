import { withDisablePortal } from '_utils/material-ui.util'
import '_utils/type-defs.util'

/** @type {MuiComponents['MuiDialog']} */
const overwrite = {}

export const MuiDialog = withDisablePortal(overwrite)
